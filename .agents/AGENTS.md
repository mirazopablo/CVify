
## Package Manager Policy
- **Strict Requirement:** ALWAYS use `pnpm` as the package manager for this project. Never use `npm` or `yarn` to install dependencies or run scripts, to prevent lockfile synchronization issues and CI/CD breaks.
